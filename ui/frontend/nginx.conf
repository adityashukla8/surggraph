server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing, if any, resolves to index.html
    # instead of a real nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
