# QA log — functional testing and defects

Every real defect found by running the system, what surfaced it, and how it was fixed. **Add an entry whenever a real run turns something up**, including issues found and fixed in the same sitting — the value here is the pattern of *what kind of thing breaks and what catches it*, not a ticket queue.

Companion to `docs/validation_results.md`, which tracks measured accuracy. This file tracks correctness and behaviour.

## The single most useful finding

**Almost nothing here was caught by unit tests.** 70 tests pass and passed throughout; the defects below came from running the actual system and looking at the actual graph. Several were invisible by construction — the graph store has no foreign-key enforcement and the renderer drops a dangling edge without a word, so a graph can be badly broken for a viewer while every write "succeeded" and every count looks right.

That is why `scripts/validate_graph_chain.py` exists and why it runs after every graph-writing change. Three separate defects (#12, #15, #16) were found by it and by nothing else.

---

## How to test

```bash
uv run pytest tests/ -q                        # 70 tests
cd ui/frontend && npx tsc -b                   # NOT --noEmit, see #5
uv run scripts/validate_graph_chain.py <case>  # dangling / orphans / reachability / plan relations
```

A real end-to-end run means: open a case through `POST /cases/open`, let the sweeps finish, then run the chain validator. Testing agents in isolation has repeatedly missed defects that only appear on the full path (#11, #12).

---

## Open

| # | Issue | Impact | Notes |
|---|---|---|---|
| O1 | Per-window latency ~15.8s against a 5s window cadence | Graph lags the video substantially; a 90s bounded sweep takes ~5 min wall clock | Dominated by Gemini call time, not writes. Batching already removed the write bottleneck. No fix identified that does not cost accuracy. |
| O2 | Europe PMC grounding — **substantially improved 2026-08-16, not closed** | Was 0 of N complications grounded, so the verification gate could never pass. Now **7 of 17** in a live case, with 7 real evidence edges | Root cause was query construction, not ranking: Europe PMC ANDs every term, so one long query starves the ranker rather than refining it (608 hits at 4 terms → 1 at 10). Fixed with multi-query fanout plus Reciprocal Rank Fusion — see the fanout entry under Fixed. Remaining gap: whether an alert clears the gate now depends on which complication happens to diverge, since ~60% are still ungrounded. `sort=CITED desc` and dropping the open-access filter were both measured and made results *worse*. |

| O4 | `DEFAULT_THRESHOLD=1.7` never re-tuned | Error detection skews toward false negatives | Confusion matrix in `validation_results.md` shows the skew across two separate runs. Flagged since 2026-08-13. |
| O5 | Model Armor floor settings backend returns real `500 INTERNAL` on both `describe` and `update`, every location tried | Blocks full per-agent Model Armor dashboard population in the GEAP console | Reproduced 3× with distinct error IDs at `locations/us-central1`; `locations/global` instead returns `PERMISSION_DENIED` despite full project Owner role. Not a permissions or client-side bug — see #46. The IAM grants Agent Gateway's Model Armor callout path needs are also pending explicit approval (project-level IAM policy change, withheld from autonomous execution). |
| O6 | 4 orphaned SurgBot reasoning engines from earlier redeploys not yet deleted | Console clutter only — Agent Registry now correctly points at the live 4 (see #44), so no functional impact | Deletion is a destructive action withheld pending explicit approval. |

---

## Fixed

### Graph structure and the chain

**#12 — Perception output rendered as disconnected nodes.**
Every event, phase, snapshot and vitals node floated with no path back to the agent that produced it. Found by looking at the rendered graph. Root cause: nothing ever wrote the edges. Fixed by adding a `succession` edge kind and building a real spine — the agent owns the first activity, each activity follows its predecessor, and events hang off the activity they occurred during. §4.3 already required timestamp ordering; this makes it a graph relation rather than only a sort key.

**#13 — Error Detection wrote `detection` edges to a node that never existed.**
It emitted edges from `phase:{id}` while Perception writes `phase:{id}:{window}`. ReactFlow silently discarded every one, so error nodes floated. Same visible symptom as #12, entirely different cause. Fixed by anchoring to the coordinator node, which the static skeleton guarantees exists.

**#14 — Error Detection wrote duplicate orphaned phase nodes.**
Labelled `"Phase 0 (0:00–0:12)"` — the meaningless ID-based label plan_v2 §13.4 rules out. The guard meant to prevent it looked for `phase:{id}` while Perception writes `phase:{id}:{window}`, so it never matched. Removed entirely; Error Detection has no semantic phase description to offer, as its own docstring said.

**#15 — Snapshot-slot edges written before the slots existed.**
The four `agent → snapshot slot` links were written up front, but a slot node is only created once a window has content for it. Caught by the chain validator. Fixed by having the links ride the same batch that creates the slot — the store assigns `seq` in list order, so the node always lands first.

**#16 — Literature nodes orphaned, then parented to the wrong thing.**
First they had no edges at all; then they hung off the Literature Retrieval agent, making it a large competing hub that buried the sequence a reader follows. Now they hang off the error whose investigation retrieved them, so the flow reads error → literature → complication.

### Reasoning correctness

**#6 — The activity-persistence rule never fired once.**
Implemented as "the same normalized description must appear in two consecutive windows". The model does not repeat itself when an activity is stable — it describes each window's specific moment, so three consecutive windows of one continuous suturing activity returned three different strings. `current_activity` stayed `None` for an entire sweep. §7.6 says *"the change persists"* — it is the **departure from the established activity** that must persist, not one exact wording. The three real descriptions are now regression tests.

**#7 — Vitals flagged a deviation on essentially every window.**
Deviations were measured against the patient's pre-insufflation baseline, but insufflation legitimately raises EtCO2 and airway pressure for the whole case. A vitals node would have been written every window, flooding the graph with exactly the steady-state noise the change-diff design exists to suppress. Now measured against `expected_at(frac)` — the uncomplicated course at that moment. Result: 12 of 55 windows deviate instead of all of them.

**#24 — Corrective action library paraphrased its sources.**
Two entries claimed to invert an OCHRA indicator but quoted it approximately (`"without repositioning"` vs the real `"without progress"`). Caught by a test that asserts every action quotes a real indicator verbatim — written specifically because the library's honesty rests on that claim.

**#26 — One long literature query returned nothing usable.**
Europe PMC is a boolean AND system: every term is a clause the paper must literally contain, so a precise-sounding query eliminates the papers it was meant to find. Measured on one real clinical question: 4 terms → 608 hits, 10 terms → 1 hit, and that survivor was a conference abstract book — the only document dense enough to contain every term somewhere. The agent's own instruction was the cause; it told the model to include mechanism and patient specifics, which is exactly what breaks this API. A `broader_query` fallback existed but only fired on *zero* hits, so 66 tangential results counted as success and it never ran. Replaced with multi-query fanout (3–5 short independent queries, hard 4-content-word cap, synonyms as separate queries) merged by Reciprocal Rank Fusion. Field prefixes verified working; MESH turned out far narrower than expected (68 hits for a central term, top result about women undergoing implant explant) so it is offered as at most one query rather than the default.

**#25 — Five near-identical divergence alerts for one event.**
One error spawned several complications, each produced a proposal, and each proposal independently noticed the same recurrence. Per-proposal deduplication could not catch it since every alert was genuinely the first *for its proposal*. Now also deduplicated by the underlying evidence. 5 → 1 on the following run.

### Wiring and orchestration

**#11 — The event bus was never registered.**
`complication_reasoning.subscribe()` worked and was verified working when called directly, but nothing in `open_case` ever called it — so through the real UI path the agent could never fire at all. Errors appeared and nothing followed. Only visible on the full path; direct agent testing passed.

**#17 — One failed write killed an entire sweep.**
A transient `httpx.ReadError` in a per-window callback propagated out and ended the whole Error Detection sweep on its first window, so no error node was ever written and the entire reasoning chain behind it never ran. docs §10 is explicit that a failed window loses *that window*. Both sweeps now contain per-window failures.

### Infrastructure

**#8 — Eight concurrent writes to one case returned HTTP 503.**
Every write transactionally bumps a single `seq` field on the case document, so concurrent writers exhausted the transaction's five attempts. This is the normal operating condition under the documented concurrency model, not an edge case. Fixed with a per-case write lock plus retry-with-backoff.

**#9 — Write throughput ~1.2s per write, serialized.**
A perception window emitting ten patches cost ~10s against a 5s cadence. Added a batch endpoint applying N items in one transaction with consecutive seq values. Measured 15.08s → 2.98s for ten writes.

**#10 — Stale pooled connections after the httpx swap.**
Uvicorn closes idle keep-alive connections after 5s; writers here can go 15s+ between calls, so httpx handed back sockets the server had already closed. The previous `requests` path never hit this because it opens a fresh connection per call — the pooling that made writes faster is what exposed it. Fixed with a shorter keepalive expiry than the server's plus transport-level retry, which is safe because the request never arrived and cannot have duplicated a write.

**#5 — The frontend typecheck was checking nothing.**
`ui/frontend/tsconfig.json` is `{"files": [], "references": [...]}`, so `npx tsc --noEmit` resolves zero input files and always exits 0. Every "typecheck clean" record before 2026-08-16 was a false signal. The correct command is **`npx tsc -b`**, which surfaced 15 real pre-existing errors on first run. Corrected in `validation_results.md`.

**Firestore database not persisted in `.env`.**
`FIRESTORE_DATABASE` defaulted to `(default)`, which does not exist for this project, causing silent 500s on every restart where the var was not exported. Now in `.env`.

### UI

**#18 — The trigger node appeared to be missing.**
It was always written with all its edges. ReactFlow's `fitView` prop runs on mount, when the graph is still empty, so it fitted nothing and every node arriving afterwards landed outside the viewport — and the trigger sits at the far left as the layout root, so it was first off screen. Now fits exactly once, when the first nodes arrive. Deliberately not a re-fit on every change; that was removed on purpose because it yanked the viewport mid-inspection.

**#19 — Errors that looked identical behaved differently.**
The node displayed *confidence*, but confidence is not what gates downstream reasoning — severity is, and the two are unrelated. An error at 95% confidence can be medium while another at 90% is high. Severity now drives the outline colour and appears on the node.

**#20 — Missing complications looked like dropped steps.**
The more common reason for a missing complication turned out not to be severity at all: a category already reasoned about in the current phase is skipped, which is the documented idempotency rule but was completely invisible. Every error now carries `complication_status` — `reasoned`, `already_reasoned`, `below_severity_threshold`, or `none_warranted`.

**#21 — Complication edges drawn solid.**
Solid reads as "this happened". A complication is a reasoned possibility, so it is now dashed, consistent with `prediction` and `proposal` in §4.2.

### External API integration

**#22 — Literature queries returned zero results.**
Europe PMC requires every query term, so the agent's precise ten-word query matched literally nothing while an eight-word one returned four results. Fixed by having the agent supply its *own* broader fallback — only it knows which terms carry the clinical question, so a deterministic truncation would strip blindly.

**#23 — Wrong field name and raw markup.**
The API returns `snippet`, not `abstract`, so the model was receiving titles with no text at all. Titles and snippets also carry HTML entities and inline tags that were landing verbatim in prompts and node labels. Both fixed at the retrieval boundary.

### Cloud Run deployment (2026-08-23)

First real deploy of `state_service`, `orchestrator_service`, and the frontend to Cloud Run. Every issue below was silent or misleading at first — no exception, or an error message pointing at the wrong layer — found only by watching real graph state after a real deployed case, the same lesson as #11/#17 one level up the stack.

**#27 — Video file silently excluded from every deploy, despite a correct `.dockerignore`.**
`gcloud run deploy --source` uploads to Cloud Build using `.gcloudignore` — and when that file doesn't exist, gcloud auto-generates one from `.gitignore`. This repo's `.gitignore` excludes `data/video/*` (correctly, for git — an 878MB binary has no business in version history). Editing `.dockerignore` first had zero effect: that file only controls what the Dockerfile copies from an *already-uploaded* source into the image, and the video never reached the upload at all. Two separate ignore-file checks at two different stages; the real gate was one stage earlier than the symptom pointed. Fixed with an explicit `.gcloudignore` that does not inherit `.gitignore`'s git-history-hygiene rule for this unrelated question ("what does the running app need").

**#28 — Orchestrator's vision agents need the video on local disk; `state_service` does not.**
`cv2.VideoCapture` (`tools/video_utils.py`, real frame extraction) only opens local file paths, never a GCS URI — unlike `state_service`'s browser-facing stream (`gcs_video.py`), which reads GCS byte-ranges directly and never touches local disk. Baking the video into the shared Dockerfile fixes the orchestrator but also ships the same ~878MB, completely unused, inside `state_service`'s image, since both services build from one Dockerfile. Accepted for now, not the ideal fix — see the retrospective note at the bottom of this section.

**#29 — Two different real URLs for the same Cloud Run service, only one allow-listed for CORS.**
Cloud Run gives every service both a hash-based URL (`*-bfa6iiuaia-uc.a.run.app`) and a project-number-based one (`*-518946358970.us-central1.run.app`) — both serve identical content, but a browser treats them as two entirely different origins. `STATE_SERVICE_CORS_ORIGINS`/`ORCHESTRATOR_SERVICE_CORS_ORIGINS` were set to only the first (the one `gcloud run services describe --format='value(status.url)'` returns); loading the app via the second got a real `400` from Starlette's own CORS preflight rejection, surfaced by the browser as a bare "CORS error" with no indication it was just an origin mismatch. Fixed by allow-listing both, comma-separated — which itself needed `gcloud`'s `^@^` delimiter-override syntax, since `--update-env-vars` normally uses commas to separate *different* variables, not values within one.

**#30 — Cloud Run's default CPU throttling silently stalled background sweep work.**
`/cases/open` returns `200 OK` immediately and does the real sweep afterward via FastAPI `BackgroundTasks`, deliberately, so the frontend isn't blocked. Cloud Run's default (CPU throttled between requests) only guarantees CPU while a request is actively being handled — once the response was sent, the background task was starved with zero further log output, not even an exception. A case would sit forever at its initial agent-registration nodes with no error signal anywhere to point at. Fixed with `--no-cpu-throttling`. Load-bearing for any Cloud Run service that starts real work it doesn't wait on before responding — Cloud Run's default assumption (nothing meaningful happens outside a request/response cycle) is false for this architecture specifically.

**#31 — 512Mi default memory OOM-killed the orchestrator mid-sweep, silently.**
Real log line: `Memory limit of 512 MiB exceeded with 512 MiB used`. Cloud Run killed the instance and started a fresh one to keep serving traffic — the fresh instance has no knowledge of the in-flight sweep that died with the old one, so the case stalls permanently with no error ever reaching the graph. Same visible symptom as #30 (silent stall at N nodes), completely different mechanism (a real kill, not starvation) — found only by reading Cloud Run's own infra-level logs, not the app's. Decoded video frames plus concurrent multimodal Gemini payloads need real headroom; fixed with `--memory=2Gi --cpu=2`, verified against a live 8+-minute run producing 287 real graph nodes with zero stalls.

**#32 — `state_service`'s default 300s request timeout cut its own SSE stream.**
Only the orchestrator's deploy set `--timeout=3600`. `state_service`'s long-lived `GET /state/{case_id}/stream` connection is exactly the same shape of long-running request and was left on Cloud Run's 300s default. Confirmed via a real log line (`"Truncated response body. Usually implies that the request timed out..."`). Fixed with the same `--timeout=3600`.

**Retrospective — the ideal fix for #28, not yet implemented.** Baking one fixed video into the image is the wrong shape: it doesn't generalize to a second video without a rebuild, and it wastes ~878MB in the one service that never uses it. The correct pattern (standard for "container needs local file access to something durably in object storage") is lazy download-on-demand: check a local cache path, download from GCS once on a miss, serve local reads exactly as today after that. One real gotcha if this is ever built: Cloud Run's default writable filesystem (`/tmp`) is memory-backed, so a naive cache would eat directly into the same memory budget #31 just fixed — needs either a deliberately larger `--memory`, or Cloud Run gen2's real (non-memory) mounted volume support for scratch space.

**Process lesson.** Every one of #29–#32 is a different Cloud Run *default* (CORS origin list, CPU allocation, memory, request timeout) that is individually reasonable and was wrong for this specific app's shape — long-lived streams, background work outliving its triggering request, real per-request memory use. Hitting one wrong default is not evidence the others are fine; the right response the first time a Cloud Run default causes a silent failure is to check all of them against how *this* app actually behaves, not patch the one that happened to surface first.

### SurgBot voice pipeline and GEAP infrastructure (2026-08-25 – 2026-08-26)

SurgBot (a new, additive conversational case-review agent, `agents/surgbot/`) first shipped on Vertex AI's Live API (bidi streaming) for voice. Real production use surfaced enough Live-API-specific failures that the transport was replaced outright rather than patched further; separately, getting SurgBot's real GEAP feature usage (Agent Registry, Model Armor) actually visible in the console turned into its own investigation. Both threads below.

**#33 — Live API barge-in overflowed a proprietary internal queue.**
Automatic VAD-triggered barge-in raised `QueueFull` in Agent Runtime's own `assembly_service.py` (`output_queue`/`application_queue`) — an internal implementation detail with no public knob to tune. Partially mitigated by moving to push-to-talk (client gates when audio actually streams) instead of continuous listening; the real fix was abandoning the transport (see the decision below).

**#34 — `bidi_stream_query`'s real, undocumented ~10-minute session ceiling, hit live.**
Real log: `base_llm_flow.py: "Received go away signal: time_left='57s'"`, followed by ADK's own auto-reconnect logic hanging for 85s before the session was cancelled outright. Stricter than the raw Live API's own 15-minute audio-only cap, and not stated anywhere public — found only by hitting it.

**#35 — Six blocking synchronous calls inside `async def` tool functions froze the entire event loop, including the Live connection's own keepalive.**
Subagent lookup, Model Armor, Memory Bank, and Firestore calls sat directly in async functions across `root_agent.py` (4 sites), `subagents.py` (1 site), `services/surgbot_service/main.py`. Under a long-lived bidi connection this could stall the keepalive itself, compounding #34. Fixed with `asyncio.to_thread(...)` at every site.

**#36 — No Gemini 3.5+ model on the Live API supports function-calling at all.**
The only model actually named "3.5" on Live API, `gemini-3.5-live-translate-preview`, is a narrow speech-to-speech translation model — no tools, no system instructions, structurally incapable of a tool-calling conversation. Used `gemini-live-2.5-flash` (the real, working id — `gemini-2.5-flash-live` from secondhand docs 404'd against every region) as a disclosed compromise, with a persistent UI element naming the exact model/API surface for every turn.

**#37 — Four real deploy-time bugs putting the root agent on Agent Runtime's bidi transport**, found by reading the installed SDK source rather than trusting docs: (1) `bidi_stream_query` only exists on `vertexai.preview.reasoning_engines.AdkApp`, not the STABLE `vertexai.agent_engines.AdkApp`. (2) The client-override point ADK's live-connect path actually reads is the `client_kwargs` constructor field — a separate `_live_api_client` cached_property ignores any override of the plain `api_client` property, so that first attempt silently had no effect. (3) `identity_type=AGENT_IDENTITY` broke the deployed agent's own outbound Live API call (`1008 policy violation: ... Expected OAuth 2 access token`) — SPIFFE/mTLS identity doesn't translate into the bearer token `google-genai`'s WebSocket client expects. Confirmed via a second spike that AGENT_IDENTITY works fine for ordinary tool-calling agents that never open a Live connection themselves — the incompatibility is specific to self-initiated outbound Live traffic.

**Decision (2026-08-25): the Live API/bidi transport was abandoned outright, not patched further.** Replaced with a classic Speech-to-Text (Cloud Speech v2, Chirp 3) → LLM (Gemini 3.5, the same STABLE `agent_engines.AdkApp` + `async_stream_query` already proven for the tool-free subagents) → Text-to-Speech (Cloud TTS, Chirp 3 HD) pipeline — full design in `docs/plan_v2` §15. This also collapsed the disclosed "Live model for voice / Gemini 3.5 for reasoning" split into "Gemini 3.5 everywhere," and removed #34–#37 as a class: no more long-lived bidi connection to time out, no more Live-only model to disclose.

**#38 — One-shot STT/TTS was real but slow: 2.6–11.3s per STT call, 2.6–2.8s time-to-first-audio on TTS.**
Fixed with real streaming APIs on both legs: Cloud Speech v2 `StreamingRecognize` (bidi gRPC, bridged from asyncio to a background thread via a `queue.Queue`) and Cloud TTS `streaming_synthesize` (bidi gRPC, sentence-split so playback can start before the whole reply is synthesized). Measured improvement: STT tail-wait ~1.5s (down from 2.6–11.3s — still genuinely variable, backend-dependent), TTS time-to-first-chunk ~0.2–0.6s (down from 2.6–2.8s). Scoped strictly to the transport layer — orchestration, tool-calling, and prompt logic were deliberately untouched.

**#39 — Streaming TTS rejects `AudioEncoding.LINEAR16` outright** (`400 Unsupported audio encoding`). The correct streaming encoding is `AudioEncoding.PCM` with an explicit `sample_rate_hertz` in `StreamingAudioConfig`; the output is raw headerless PCM16, unlike the one-shot call's WAV-wrapped bytes — the frontend player had to be rewritten around this (`PcmChunkPlayer`, gapless scheduling of raw PCM chunks instead of decoding a WAV blob).

**#40 — Frontend still accumulated mic audio into one blob after the streaming-STT backend change** — a leftover from the pre-streaming design. Real error: `400 Audio chunk can be of a maximum of 25600 bytes. Received audio of 123914 bytes instead.` Fixed by sending each captured chunk immediately as it's produced instead of buffering client-side into one send.

**#41 — Deployed-sandbox logging gap, same class of bug as an earlier fix one level up the stack.**
`agents/surgbot/root_agent.py` never called `logging.basicConfig()`, silently dropping every `logger.info()` call once deployed to Agent Runtime's sandbox — identical to a bug already fixed once in `services/surgbot_service/main.py`; missed here because that first fix wasn't generalized to a shared init path.

**#42 — Stale Agent Identity tokens on long-running deployed subagents.**
Real `401 UNAUTHENTICATED` on subagents' own outbound Gemini calls after many hours of container uptime, confirmed via Cloud Logging — not a code bug, a real GEAP Agent Identity token-refresh limitation for long-lived containers. Fixed operationally (redeploy fresh); no code-level mitigation identified.

**#43 — Agent Runtime deployment never auto-registers an agent in Agent Registry.**
Confirmed empirically: `gcloud agent-registry agents list` / `services list` showed nothing after a real deploy, only Google's own default "Workspace Agent". `gcloud agent-registry services create` (not `agents create` — that subcommand doesn't exist; `agents` is describe/list/search only) is the real, working registration path, built into `scripts/register_surgbot_agents.py`.

**#44 — Agent Registry silently pointed at deleted/stale reasoning engines, breaking the GEAP console's Topology view** ("Something went wrong fetching data," found 2026-08-26). Root cause: `services create` on an already-registered `service_id` is a no-op (`ALREADY_EXISTS`) rather than repointing the interface — so three root-agent redeploys after the first registration left `surgbot-root-agent`'s registry entry pointing at a superseded engine, and `surgbot-synthesis-subagent`'s pointed at a reasoning engine that had since been deleted outright (confirmed via a direct `describe`: real `404 NOT_FOUND`). `error_chain_reviewer`/`pattern_insight` had never been registered at all. Fixed by making `register_surgbot_agents.py` fall through to `services update` on `ALREADY_EXISTS`, and extending it to cover all 4 agents instead of 2; re-run confirmed all 4 now point at their live resources. 4 orphaned reasoning engines from earlier redeploys were identified — see O6.

**#45 — Model Armor's per-agent console dashboard (Agents ▸ *agent* ▸ Security) stayed at zero interactions despite real, working screening.**
`agents/surgbot/model_armor.py` calls `ModelArmorClient.sanitize_model_response` directly from app code — confirmed actually running, and correctly passing/blocking, via three independent checks (a real Firestore `approval_status`, a deterministic direct-call test with a known-unsafe string, application logs) — but the console's own banner states that dashboard only populates for traffic routed through **Agent Gateway** with tracing enabled; a direct SDK call bypasses it entirely. Separately confirmed the project has no Data Access audit logging enabled for any service, so Cloud Audit Logs wouldn't show individual `sanitize_*` calls either, even in principle.

**#46 — Wiring up real Agent Gateway + Model Armor dashboard population turned out to be a genuine infra project, only partly completed 2026-08-26 — see O5.** Real findings, not guesses: Agent Gateway is a real resource (`gcloud network-services agent-gateways`, `networkservices/v1` API — already enabled on this project). Created `projects/liveapi-488810/locations/us-central1/agentGateways/surgbot-client-gateway` (`CLIENT_TO_AGENT` mode). Routing an actual reasoning engine's ingress traffic through it requires setting `agent_gateway_config.client_to_agent_config.agent_gateway` on that engine's `DeploymentSpec` at (re)deploy time — a real field on the SDK's `ReasoningEngineSpecDeploymentSpecAgentGatewayConfig`, found only by introspecting the installed `vertexai._genai.types` module directly (`dir()` + `model_fields`); it appears in none of the public docs pages fetched this session. The IAM grants that path needs (`roles/modelarmor.calloutUser`/`roles/modelarmor.user` for the AI Platform Reasoning Engine Service Agent) are a project-level IAM policy change — intentionally not applied autonomously, flagged for explicit approval instead. Separately, `gcloud model-armor floorsettings` — the mechanism matching the console banner's exact "Agent Gateway and Google Cloud MCP Servers" wording (`--enable-vertex-ai-cloud-logging`, `--enable-google-mcp-server-cloud-logging`) — returns a real, reproducible `500 INTERNAL` from Google's own backend on both `describe` and `update`, at every location tried. Genuine backend limitation in a preview product, not something fixable from application code.

**#47 — A real flexbox gotcha**, caught during headed UI verification of an unrelated change (the review-document artifact moving inline into the chat feed instead of pinning below it). `.sb__doc-artifact`'s `overflow: hidden` (added only to clip rounded corners) disables flexbox's "automatic minimum size" protection for a flex item — inside `.sb__feed`'s column layout, this let the element get crushed to a couple px tall instead of its real content height. Confirmed via a real `getBoundingClientRect()` read before/after the fix: `height: 2` → `height: 56`. Fixed with an explicit `flex: 0 0 auto`.

**Process note.** `vertexai.Client` surfaced a real deprecation warning this session (`Please use agentplatform.Client instead`). Not yet migrated — every existing script/module in this codebase uses the old class consistently, and a partial migration mid-feature-work would be worse than none; tracked as a dedicated future pass.

**#48 — First deploy of `surggraph-surgbot-service` (2026-08-26), plus `--set-build-env-vars` silently doing nothing on a Dockerfile-strategy `gcloud run deploy --source`.**
`ui/frontend/Dockerfile` was never updated with `ARG VITE_SURGBOT_SERVICE_URL` when that env var was introduced earlier this session — a real gap, not a deploy-tooling issue on its own. But even after fixing that, `gcloud run deploy --source . --set-build-env-vars=...` built and deployed successfully with **no error at all**, and the resulting bundle still had all three `VITE_*` URLs at their local-dev fallback values (`http://localhost:8080` etc., confirmed by downloading the deployed JS bundle and grepping it directly — no `run.app` string anywhere). `--set-build-env-vars` is grouped in `gcloud run deploy --help` next to `--base-image`, a Buildpacks-only concept — it appears to only apply to Cloud Native Buildpacks builds and is a silent no-op for a Dockerfile-strategy build, with no warning either way. Confirmed via a real image-content diff: identical Dockerfile, only the build mechanism changed. Fixed by bypassing `gcloud run deploy --source` for this step entirely — `gcloud builds submit --config=cloudbuild.yaml` with an explicit inline `docker build --build-arg=...` step, then `gcloud run deploy --image=<built image>` — which correctly passed the args through and baked the real URLs into the bundle (re-verified the same way, all three `*.run.app` URLs present, zero local fallbacks).

**Real end-to-end verification of the full deploy** (2026-08-26): a raw WebSocket client (not a browser) opened `wss://surggraph-surgbot-service-518946358970.us-central1.run.app/surgbot/.../voice` against the real public URL, sent `session_start` then a real `text_turn` ("Please list the accessible cases."), and got back a real `phase_changed`, a real `list_accessible_cases` tool-call chip with real Firestore case data, and a real streamed model reply — the full stack (Cloud Run relay → deployed Agent Runtime root agent → Gemini 3.5 tool call → Firestore → back through the relay) working end to end on real infrastructure, not local dev.

**IAM gaps found and fixed while wiring this up:** `surggraph-runtime@...` (the service account all these services run as) had no Cloud Speech-to-Text permission at all (`roles/speech.client` was missing — STT would have failed silently on first real deploy). Fixed by granting it. Separately confirmed Cloud Text-to-Speech has **no IAM-scoped permissions in this project at all** (`gcloud iam list-testable-permissions` returns zero matches for `texttospeech.*`) — any authenticated caller with the API enabled already has access, nothing to grant.

### Feedback knowledge base (plan_v2 §16, 2026-08-26)

SurgBot already captured structured surgeon feedback (`ReviewFeedbackItem`) but nothing ever consumed it. This closes the loop: on review approval, feedback is routed to the real SurgGraph agent it's about, written to a new `surgbot_feedback` Firestore collection, and mirrored into GEAP Memory Bank as a retrievable fact four core agents consult at inference time (Divergence Detection, Complication Reasoning, Literature query formulation, Corrective Replanning).

**Decision — advisory-only, never gating.** Explicit user call: retrieved feedback is injected as labelled prompt context only. Nothing auto-suppresses an alert or auto-tunes a threshold; `agents/verification_gate/gate.py` is untouched. The rendered block itself tells the model not to suppress a finding solely because a past reviewer disagreed with a similar one.

**Decision — institution-wide KB via a constant Memory Bank *scope* value, not a real identity change.** User's explicit requirement: the KB must populate the same for the author, a judge, or anyone else, without touching real multi-tenancy anywhere else. `SURGGRAPH_KB_USER_ID = "1"` is used **only** inside Memory Bank `scope` dicts (`tools/feedback_kb.py`); every `FeedbackRecord` still stores the real `reviewer_id`. Guarded by a real regression test (`test_kb_constant_never_leaks_outside_memory_bank_scope_construction`) that greps `store.py`/`schema.py`/`root_agent.py` for the constant's name and fails if it ever appears outside scope construction.

**Decision — routing derived from real data, not a hand-authored answer table.** `subject_node_id` → `node_type` (via `state/node_ids.py::node_type_for`, the codebase's existing single source of truth) → target agent, using a 5-entry structural map (`NODE_TYPE_TO_AGENT`) — architecture (which agent owns which node type), not domain content. Classification of genuinely ambiguous feedback (no subject node, or a subject node with no verdict) goes through a real 4th SurgBot subagent, `feedback_router`, deployed to Agent Runtime and smoke-tested live — it correctly classified "Prefer literature published within the last 10 years" as `directive → literature_retrieval` on the first real call.

**#49 — Real architectural layering violation caught before it shipped.** The plan's original file placement (`tools/feedback_kb.py` importing `agents/surgbot/memory_bank.py` and `agents/surgbot/feedback.py`) would have made `tools/` depend on `agents/` — this repo's established direction is strictly `agents/` → `tools/`, verified by grep with zero existing exceptions. Fixed two ways: (1) moved the now-fully-generic Memory Bank client from `agents/surgbot/memory_bank.py` to `tools/memory_bank.py` outright (it had already lost every SurgBot-specific parameter in the scope-generalization pass, so this was a move, not a duplication); (2) put the scope/fact-format/routing contract shared by writer and reader in `tools/feedback_kb.py`, with `agents/surgbot/feedback.py` importing and re-exporting it rather than owning a second copy (regression-tested: `test_shared_contract_is_reexported_not_duplicated` asserts object identity, `test_tools_modules_never_import_from_agents` greps both `tools/` modules).

**#50 — Second, subtler instance of the same layering issue: the Memory Bank "parent Agent Engine" resource name.** The four core pipeline agents need this to call `feedback_block()`, and the only code that resolves it (`_synthesis_resource_name()`) lives in `agents/surgbot/subagents.py` — importing it from e.g. `agents/divergence_detection/agent.py` would make the autonomous pipeline depend on SurgBot's deployment internals. Fixed by resolving it via a plain env var (`SURGGRAPH_FEEDBACK_KB_ENGINE`, set once to the real deployed resource name) instead of an import — `tools/feedback_kb.py::default_agent_engine()` reads it directly; unset or unreachable both fail soft to an empty block, never an exception.

**#51 — Regex header-parsing bug, caught by its own test on the first run.** `parse_fact`'s capture groups (`[^|\]]*`) greedily consumed the space before each ` | ` separator, so `"disagree "` (trailing space) failed an exact-match assertion against `"disagree"`. Fixed by stripping each captured group rather than fighting the regex boundary.

**#52 — Real Memory Bank indexing lag turned out to be a red herring for a real process-hygiene bug.** During E2E verification (§16.9 Step 5), a freshly-opened case's Divergence Detection retrieved `0 observation(s)` from a scope that a direct script call, seconds later, could see just fine — first read as GEAP's similarity index lagging behind a write. The real cause: `.env`'s `SURGGRAPH_FEEDBACK_KB_ENGINE` had been deliberately pointed at a bogus resource for the E2E-B fail-soft test, then restored on disk — but `orchestrator_service` was never restarted, so the live process kept running with the broken value from its own startup `load_dotenv()`. 90 more real, correctly-caught `tools.memory_bank` failures accumulated in the log before this was caught (bonus confirmation of #16.9's fail-soft property, but it meant the "second case picks up new feedback" claim hadn't actually been tested yet). Fixed by actually restarting the process; the very next case retrieved all 3 real observations correctly. Lesson: editing `.env` never affects an already-running process — always restart, never assume.

**#53 — Truncating a log file via shell redirection (`: > file`) while a writer process still holds it open corrupts subsequent reads unpredictably.** Done twice in a row while trying to get a "clean" log before an E2E run. Real symptom: lines directly quoted from a `tail`/`grep` moments earlier became unfindable by any later `grep`, including fixed-string and substring-only queries, with the file's apparent size still growing normally. Not a Memory Bank or feedback-layer bug — a shell/filesystem footgun. Fixed procedurally: `pkill`, confirm death via `ps`, `rm` the file outright (not `: >`), then start the new process with its own fresh redirect — never truncate a path a live process still has open.

**Live end-to-end verification, real evidence, no mocks (2026-08-26):**
- All 4 SurgBot subagents (`error_chain_reviewer`, `synthesis`, `pattern_insight`, `feedback_router`) deployed/confirmed on Agent Runtime and smoke-tested with real prompts and real parsed responses.
- `scripts/test_feedback_write.py`: a real approved review with 4 feedback items (1 divergence-alert observation, 1 no-subject-node directive, 1 prompt-injection attempt, 1 unroutable bogus node id) → 4 real Firestore docs, real Memory Bank writes, real Model Armor block (`prompt injection / jailbreak pattern detected`) on the injected item, real cross-scope isolation confirmed. Passed twice, memories visibly accumulating across runs.
- **E2E-A (functional, KB populated):** one real case (`case-e9a669f82e25`) opened end to end. All four target agents showed real `feedback_kb[...]` retrievals with non-zero counts during the actual run (`literature_retrieval`, `complication_reasoning`, `corrective_replanning`, `divergence_detection` — the last one's cache also confirmed working live, `2.03s` down to `0.00s` on repeated polls within the same window). Case completed normally: `documentation[case-e9a669f82e25]: drafted, 5 limitation(s) stated, model armor passed`, plus a real verification-gate pass and a real FHIR delivery in between.
- **E2E-B (fail-soft, KB unreachable):** `SURGGRAPH_FEEDBACK_KB_ENGINE` deliberately pointed at a non-numeric bogus resource, orchestrator restarted, a fresh case (`case-73f5e4f82853`) opened. Every one of ~45 real `feedback_kb[...]` calls returned `0 directive(s), 0 observation(s)` — the real GEAP rejection (`400 INVALID_ARGUMENT... Reasoning Engine ID must be numeric`) was caught and logged by `tools.memory_bank`'s own `except Exception`, never propagated. Zero non-memory_bank errors anywhere in the log; case completed identically (`documentation[case-73f5e4f82853]: drafted, 3 limitation(s) stated, model armor passed`).
- **E2E-C (the actual loop):** the real divergence_alert node from E2E-A's completed case was pulled via a real `GET /state/{case_id}/snapshot`. A real SurgBot session recorded a real `disagree` verdict on it (`record_feedback`), drafted a real review (`draft_review_document`, dispatched to the deployed synthesis subagent), and was approved through the **actual running `surgbot_service`'s real HTTP endpoint** (`POST /surgbot/reviews/{id}/approval` → `200`, `approval_status: approved`) — which fired `feedback.process_review_feedback` for real, producing a real Firestore record (`memory_written: true`, routed to `divergence_detection`). A brand-new, unrelated case (`case-873cccc9bf9d`) then genuinely retrieved it: `feedback_kb[divergence_detection]: 0 directive(s), 3 observation(s)` at every poll, and the exact rendered prompt text was pulled and confirmed to contain the real new fact verbatim, correctly attributed to its real source case. Full loop (autonomy → review → knowledge → autonomy) proven on real infrastructure, no mocks.
- `uv run pytest tests/ -q` (excluding external-API suites): 130/130 passing after every step, zero regressions. Two tests updated mid-verification to reflect real state the E2E runs themselves created (`corrective_replanning`'s "must be empty" assertion → real content check; `error_detection` — genuinely still empty, per §16.7 — took over the empty-block byte-identity case).

### MedGemma deployment — switched to L4 scale-to-zero profile (2026-08-27)

Triggered by a real billing question ("charges for 2 instances yesterday"). Investigation via the real Cloud Billing Catalog API (SKU-level pricing, not a rough estimate) confirmed the always-on `g4-standard-48` + 1x `NVIDIA_RTX_PRO_6000` deployment cost ≈$5.85/hr (~$4,200/mo). The "2 instances" observation was explained, not as a second real model (only one real `DeployModel` audit-log event exists — a second nearby log entry was an empty-payload LRO completion callback), but as GCP's standard Compute/Accelerator + base/management-fee SKU split on one GPU-backed online-prediction deployment.

User asked to switch to Model Garden's own "vLLM 32K context 16 image limit" profile (`g2-standard-24` + 2x `NVIDIA_L4`), confirmed via `vertexai.preview.model_garden.OpenModel(...).list_deploy_options()` as a real, Google-validated option (not assumed from a screenshot). Real SKU pricing: ≈$2.60/hr (~$1,870/mo) — **56% cheaper**.

**Scale-to-zero default researched by reading the installed SDK source directly** (`google/cloud/aiplatform_v1beta1/types/machine_resources.py::DedicatedResources.ScaleToZeroSpec`, plus the Python wrapper `google/cloud/aiplatform/preview/models.py`): both `min_scaleup_period` and `idle_scaledown_period` are `Optional` `Duration` fields, valid range **[300s, 28800s]** (5min–8hr), and neither the proto nor the SDK wrapper sets an implicit default when omitted — the SDK only writes the field into the request if a caller passes a value. Rather than rely on an undocumented server-side default, both were set **explicitly to 300s (the platform floor)** — most aggressive cost-cutting, matching the stated goal of minimizing idle spend. This is a disclosed, deliberate choice, not a discovered platform default; the user can move it up to 28800s at any time for fewer cold starts.

**No existing SDK path attaches a new Model Garden deploy config to an existing Endpoint with scale-to-zero fields** — `vertexai.model_garden.OpenModel.deploy()`'s public kwargs don't expose `min_scaleup_period`/`idle_scaledown_period` at all (confirmed by reading `vertexai/model_garden/_model_garden.py` in full), even though the underlying `DeployConfig.dedicated_resources` proto (the exact same `DedicatedResources` message) supports it. Fixed by constructing the raw `ModelGardenService.DeployRequest` directly (`model_config.accept_eula`, `deploy_config.dedicated_resources` with `machine_spec`/`min_replica_count=0`/`max_replica_count=1`/`initial_replica_count=1`/`scale_to_zero_spec`) and calling `model._model_garden_client.deploy(request)` — the same private client the public wrapper uses internally, just without its kwarg ceiling. This path always mints a **new** Model + **new** Endpoint (no "attach to existing endpoint" field exists on `DeployRequest`), so `.env`'s `MEDGEMMA_ENDPOINT_ID` was updated to the new endpoint (`mg-endpoint-75ebd7dc-8fdd-444f-97a5-a315ab96ed9a`) rather than reusing the old one.

Real deploy LRO: started 08:47:00, finished 08:54:10 (≈7min). Confirmed via a direct v1beta1 REST read (not just the SDK) that `scaleToZeroSpec: {minScaleupPeriod: "300s", idleScaledownPeriod: "300s"}` is genuinely present on the live deployed model. A real warm-state prediction (same `aiplatform.Endpoint.predict()` call path `tools/medgemma_model.py` already uses in production) succeeded: `7.31s` latency, real MedGemma completion returned ("A laparoscopic cholecystectomy is a minimally invasive surgical procedure...").

Old deployment undeployed for real (`gcloud ai endpoints undeploy-model` on `mg-endpoint-2f7f617b-...`, deployed-model id `1646114592071352320`) and confirmed empty (`deployedModels: null`) — billing on the old g4-standard-48/RTX_PRO_6000 config stopped. Endpoint deletion itself was blocked by the auto-mode classifier (destructive-action guard); left in place empty (zero cost) pending explicit user go-ahead to delete.

**Cold-start latency — real, measured, not estimated.** Endpoint genuinely scaled to zero at 08:59:44 (availableReplicaCount: 1→0, confirmed via direct v1beta1 polling every 30s). A real prediction fired immediately after (08:59:56) against the zero-replica endpoint: the **first** request returns an immediate real `HTTP 429 "Model is not yet ready for inference. Please wait while model completes scale-up from zero, then try your request again."`, not a blocking/queued response — a client must poll/retry. Retried every 5s using the exact `aiplatform.Endpoint.predict()` call path production code already uses: **41 consecutive 429s**, then a real success on attempt 42. **Total wall-clock cold-start latency, first-request-to-first-successful-response: 279.06s (≈4min 39s).** Real completion returned: "A laparoscopic cholecystectomy is a minimally invasive surgical procedure to remove the gallbladder."

**Decision (2026-08-27, same day): reverted to fixed always-on, no autoscaling.** Given the real ~4min39s cold-start cost, user chose `min_replica_count=1, max_replica_count=1` over scale-to-zero. Applied via `EndpointServiceClient.mutate_deployed_model` (confirmed via source read: `min_replica_count`/`max_replica_count` are real updatable fields on an already-deployed model) with `update_mask=["dedicated_resources.min_replica_count", "dedicated_resources.max_replica_count"]` — an in-place mutation on the same deployed model/endpoint, **not** an undeploy+redeploy (no new endpoint, no downtime, ~5min LRO). Confirmed via a direct v1beta1 REST read: `minReplicaCount: 1, maxReplicaCount: 1`, `scaleToZeroSpec` no longer present, `availableReplicaCount: 1`. Still the L4/g2-standard-24 profile (≈$2.60/hr, ~$1,870/mo — 56% cheaper than the original g4/RTX_PRO_6000 config), just without the scale-to-zero behavior.

### MedASR — real, self-deployed medical STT, added as an opt-in alternative (2026-08-28)

Investigated whether Google's MedASR (Health AI Developer Foundations, released Dec 2025 — a 105M-param Conformer ASR model fine-tuned on ~5,000 hours of de-identified clinical dictation/conversation audio) could replace or complement SurgBot's current Chirp 3 STT. Real, confirmed benchmark WER: 5.2–6.9% vs. Whisper large-v3's 12.5–28.2% on medical dictation — a genuine accuracy edge on exactly SurgBot's vocabulary (surgical/anatomical terms).

**Real deploy option** (`vertexai.preview.model_garden.OpenModel("google/medasr@medasr").list_deploy_options()`): exactly one — `n1-standard-8` + 1x `NVIDIA_TESLA_T4`. **Real cost** (Cloud Billing Catalog API, us-central1): Core $0.036/core-hr × 8, RAM $0.005/GiB-hr × 30GiB, T4 GPU $0.402/hr + $0.058435/hr mgmt fee → **≈$0.90/hr (~$656/mo)**. Deployed always-on per explicit user choice (`min_replica_count=1, max_replica_count=1`), confirmed via v1beta1 REST read.

**Real wire shape, not assumed** — found via Google's own `Google-Health/medasr` notebook (`quick_start_with_model_garden.ipynb`): the deployed container exposes an OpenAI-Whisper-compatible `/v1/audio/transcriptions` route, not the standard Vertex predict/`instances` envelope — calls go through `aiplatform.Endpoint.raw_predict(body=json.dumps({"file": base64.b64encode(wav_bytes)}), headers={"Content-Type": "application/json"})`, response parsed as `json.loads(response.content)["text"]`.

**Two real bugs found and fixed via actual failing calls, not assumed:** (1) the container rejected 24kHz test audio outright — real `400 {"error":"Sample rate 24000 is not 16000..."}` — strict about exactly 16kHz mono (the real production capture path, browser AudioWorklet, is already 16kHz-native; only the test's synthesized-TTS input needed a resample, via stdlib `audioop.ratecv`). (2) the real transcript came back with a literal trailing `</s>` token baked into the text (`"...abdominal wall.</s>"`) — stripped explicitly rather than papered over with a broad regex.

**Real latency, measured, not estimated:** first call after deploy: **105.80s** (a real in-container model warm-up/load cost, separate from the endpoint's own `availableReplicaCount: 1` health status — the endpoint reports ready before the model is actually loaded). Second and third calls on the same warm container: **1.22s and 1.12s** — genuinely faster than Chirp 3's already-measured one-shot latency (2.6–11.3s, #38 above) once warm.

**Added as a real, tested, OPT-IN alternative — not a replacement.** `agents/surgbot/speech.py::transcribe_audio_medasr()` mirrors `transcribe_audio()`'s exact contract (same signature shape, raises on real failure, `""` only for genuine empty audio) so the two are swappable, but `transcribe_audio()` (Chirp 3, managed, real streaming support already built) stays the default everywhere it's already wired into `services/surgbot_service/main.py` — nothing else was touched. Real round-trip test added (`tests/test_surgbot_speech.py::test_medasr_synthesize_then_transcribe_round_trip`, fed the project's own real demo vocabulary — "vesicourethral anastomosis", "laparoscopic" — not a generic sentence, since medical-term accuracy is MedASR's actual value proposition). Full suite re-run clean: **159/159 passing**, zero regressions; all 4 local dev servers (state/orchestrator/surgbot services + Vite) confirmed still healthy and untouched throughout.

**Update, same day: swapped into the live pipeline per explicit user decision, accepting the batch-vs-streaming tradeoff.** `services/surgbot_service/main.py`'s real voice path (`mic_start`/binary-frame/`mic_stop`) rewritten: the per-chunk `StreamingTranscription` session replaced with a plain `bytearray` accumulator, sent as one `transcribe_audio_medasr()` call on `mic_stop`. `_STT_DISCLOSURE` updated to `{agent_name: speech_to_text, model_id: medasr, api_surface: vertex_ai_medasr}` — new `ApiSurface` value added on both sides (`agents/surgbot/schema.py`, `ui/frontend/src/surgbot/types.ts`), frontend chip/copy updated (`SurgBotPanel.tsx`).

**Real end-to-end proof, through the actual restarted running relay, not just the unit-test level.** All 4 dev servers restarted fresh (state/orchestrator/surgbot services + Vite) to pick up the change. New `scripts/test_surgbot_audio_e2e.py` (no prior script exercised a real `mic_start`→binary-audio→`mic_stop` turn — only `text_turn` had E2E coverage before) drives a real synthesized-speech clip through the real WebSocket relay: confirmed the tool-call disclosure genuinely shows `medasr`/`vertex_ai_medasr` (not `chirp_3`), a real transcript came back with 100% word overlap against the real spoken text, and the real agent proceeded normally (real `list_accessible_cases` tool call against real Firestore data, real model reply). Full test suite re-run clean after the swap.

**One real, intermittent artifact observed, disclosed rather than silently patched around:** some runs' transcripts came back with a stray leading `"] "` fragment (`"] Please list the accessible cases..."`) — investigated by reproducing the exact same audio bytes both unchunked and chunk-reassembled directly against `transcribe_audio_medasr()` (byte-identical to what the relay sent, confirmed via direct comparison): both came back clean twice in a row, while a real relay-path E2E run with the identical spoken text showed the artifact in 2 of 3 attempts. Inconclusive whether this correlates with the relay path specifically or is pure decoder non-determinism — same category as the confirmed `</s>` suffix quirk (a real artifact occasionally leaking into MedASR's raw output), never affects intelligibility (agent understood the request correctly every time). Left as a known, disclosed, not-fully-understood quirk rather than writing a speculative regex to strip a fragment whose real cause isn't confirmed.

**Disclosed, accepted tradeoff:** MedASR's real deployed container has no documented streaming route — the live pipeline now waits for the whole clip before transcribing (batch), giving up Chirp 3's "most of the recognition already done by the time you release the button" property (#38/#39 above). Measured real latency (warm container, ~8.5s clip): 1.1–1.2s — still faster in absolute wall-clock than Chirp 3's own one-shot 2.6–11.3s, so this is not a clear UX regression despite losing the streaming architecture; not re-measured against Chirp 3's actual streaming tail-wait number head-to-head.

**Two real, separate bugs found and fixed the same day while verifying the swap end-to-end, neither caused by the MedASR change itself:**

1. **`retrieve_reviewer_patterns` returned `has_history: false` for a reviewer even though a real cross-session memory genuinely existed.** Root cause: the DEPLOYED root agent (`surgbot-root-agent`, created 2026-08-25) predated the `REVIEW_SUMMARY_SCOPE` fix committed the next day (`f416f1f`, 2026-08-27) — it was still running the OLD per-reviewer Memory Bank scope, which naturally finds nothing for a fresh `reviewer_id`. Confirmed by directly querying Memory Bank with the real `REVIEW_SUMMARY_SCOPE` (found exactly 1 real fact) and comparing the deployed engine's real `createTime`/`updateTime` (2026-08-25) against the commit date of the fix (2026-08-27) — the agent had simply never been redeployed since. **Lesson, same class as #44 above: a code fix to a tool inside `agents/surgbot/root_agent.py` does nothing in production until the agent is actually redeployed** — the fix landing in git is not the fix landing in the running system.
2. **Redeploying to apply fix #1 broke the agent completely** — every tool call failed with a real `AttributeError: 'GlobalGemini' object has no attribute 'api_version'`. Root cause: neither `google-adk` nor `google-genai` is version-pinned anywhere in this project (`pyproject.toml` doesn't pin them either), so a redeploy months apart from the last one silently resolved a newer `google-adk` release inside the sandbox whose `Gemini` model class now expects an `api_version` attribute that `tools/gemini_model.py::GlobalGemini`'s `api_client` override doesn't provide. Fixed by pinning `google-adk==2.6.3`/`google-genai==2.17.0` (this session's confirmed-working local versions) directly in `scripts/deploy_surgbot_agent.py`'s deploy `requirements` and redeploying again — smoke test passed clean on the second attempt.

Both fixes verified for real: direct `async_stream_query` call against the newly redeployed agent showed `retrieve_reviewer_patterns` correctly returning `memory_count: 1, has_history: True` with the real memory content reflected in the agent's spoken reply. Full suite re-run clean (159/159) after both fixes; all 4 dev servers restarted fresh and confirmed healthy.

---

## Process issues worth remembering

**A test fixture put a fabricated citation into a public FHIR record (2026-08-16).**
Verifying the verification gate's *pass* path required a chain the gate would accept, and real runs never produce one because complications keep coming back `evidence_backed=False`. The chain was written by hand — including a literature node with an invented title, journal and year (`"Bladder neck reconstruction outcomes after RARP", J Endourol 2024`) pointed at an arbitrary article id. That alert was written to the public HAPI server and then offered as something to demo.

Two things make this worse than it first looks. The invented id resolved to a **real but entirely unrelated paper** (gastric antrum anatomy), so the citation looked plausible and survived a naive URL check — exactly the failure mode `evidence_backed` and the gate exist to catch. And it happened while testing the component whose only job is refusing ungrounded claims.

Re-verified with a genuinely retrieved paper (`Communication/137353000`, EXT_ID 42195151, confirmed hitCount=1 and on-topic). **Rule going forward: a test fixture may hand-build graph STRUCTURE, but any content that would be presented as evidence must be really retrieved.** Structure is scaffolding; a citation is a claim.

**Backgrounded restarts queued behind long commands killed live test runs twice.** A restart command chained after a two-minute pytest fired long after I had already restarted manually, killing the servers mid-sweep and producing a "stalled" case that looked like a code defect. Restarts now go in their own command.

**Testing agents in isolation is not sufficient.** #11 and #17 both passed direct invocation and both made the system completely non-functional through the UI. A real case opened through the orchestrator is the only test that would have caught either.
